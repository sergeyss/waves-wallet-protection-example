{-# STDLIB_VERSION 4 #-}
{-# CONTENT_TYPE EXPRESSION #-}
{-# SCRIPT_TYPE ACCOUNT #-}

let secPublicKey = base58'2PNDHFzF3iRRcLfRd7g4J3DXqhNrDQ6eSuAbpwwPZsnC'
let txSig = sigVerify(tx.bodyBytes, tx.proofs[0], tx.senderPublicKey)
let secSig = sigVerify(tx.proofs[1], tx.proofs[2], secPublicKey)
txSig && secSig
